{namespace example.templates}

/**
 * @param meaningOfLife
 * @param weekdays
 */
{template .base}
<div>{msg desc="meaning"}The meaning of life is: {$meaningOfLife}.{/msg}</div>

<ul>
{foreach $weekday in $weekdays}
  <li>{strSub($weekday, 0, 3)}
{/foreach}
</ul>

{call .listTemplate}
  {param items: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'] /}
{/call}
{/template}

// TODO: Figure out why the following does not work:
// {foreach $item in list('Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat')}
//   <li>{$item}
// {/foreach}

/** @param items */
{template .listTemplate}
<ul>
{foreach $item in $items}
  <li>{$item}
{/foreach}
</ul>
{/template}
